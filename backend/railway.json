{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node setup-db.js && node migrate-db.js && node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
